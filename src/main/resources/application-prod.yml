# 线上配置文件
server:
  port: 8101
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://yyyai.xyz:3306/ybi
    username: root_yu
    password: fuckmysql
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: yyyai.xyz
    port: 6379
    timeout: 5000
    password: fuckredis
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''
# 接口文档配置
knife4j:
  basic:
    enable: true
    username: root_knife
    password: plzknife_145
